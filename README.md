# RINBŌ Ichiba — rinbo.store

| Carpeta     | Qué es                                   | Dónde se publica |
|-------------|------------------------------------------|------------------|
| `web/`      | Sitio público (HTML/CSS/JS, sin build)   | GitHub Pages → rinbo.store (workflow `.github/workflows/pages.yml`) · vistas previas en Cloudflare Pages (rinbo-web.pages.dev) |
| `admin/`    | RINBŌ Admin (fase 3)                     | Cloudflare Pages + Access |
| `supabase/` | Base de datos (fase 2)                   | — |
| `docs/`     | Plan y documentación                     | — |

Probar la web localmente: `cd web && python3 -m http.server`.
Ver `CLAUDE.md` y `docs/PLAN.md`.
